name: weather_core
description: Core utilities and infrastructure for weather app
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.3.2
  logger: ^2.0.2+1
  equatable: ^2.0.5
  get_it: ^8.0.0
  injectable: ^2.4.2
  weather_localization:
    path: ../weather_localization

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  injectable_generator: ^2.4.1
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
